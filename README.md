# Cornerstone Code Blocks

## Building

Requires

- `npm i -g esbuild`
- `npm i -g node-sass`

Building

```sh
npm run build;

# Or individually
npm run build-css
npm run build-js

# Scripts are also in ./bin/
```
